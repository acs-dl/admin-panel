export enum REQUEST_ORDER {
  empty = '',
  asc = 'asc',
  desc = 'desc',
}

export enum UNVERIFIED_USER_SORTING_PARAMS {
  empty = '',
  createdAt = 'created_at',
  username = 'username',
  name = 'name',
}
