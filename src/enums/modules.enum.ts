export enum MODULES {
  gitlab = 'gitlab',
  github = 'github',
  email = 'mail',
  telegram = 'telegram',
}

export enum REFRESH_MODULE_STEPS {
  all = 'all',
  module = 'module',
  submodule = 'submodule',
}
